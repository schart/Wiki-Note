"use strict";
import express, { Request, Response, NextFunction, Application, Router } from 'express';

export const LoginRoute: Router = express.Router();
export const RegisterRoute: Router = express.Router();


//import Note_sub_proccess from '../controllers/Note_C';


// midwares
//import * as user_midwares from '../middleware/User_M';



//RegisterRoute.post('/register-user',  /* */, (req: Request, res: Response, next: NextFunction) => { });
//LoginRoute.post('/login-user', /* */, (req: Request, res: Response, next: NextFunction) => { });
//LoginRoute.get('/logout-user', /* */, (req: Request, res: Response, next: NextFunction) => { });

